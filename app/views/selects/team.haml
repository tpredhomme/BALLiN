.card.card-sp
    .card-block
    
        .card-title= "Choisis ton équipe"
        .card-subtitle= "Dis-nous pour qui tu joues"


.card.card-sp
    .card-block

        .row.mb-0
            .col
                - @teams.each do |team|
                    = link_to selects_path(club_id: params[:id], team_id: team.id), method: :post do
                        .panel-gray
                            %div= "#{team.name}"
                            %small= "#{team.players.count} joueurs"
